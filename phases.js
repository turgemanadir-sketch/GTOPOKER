{{ ... }}
    // Trigger enhanced confetti with poker cards
    triggerPhaseCompletionConfetti();
    
    // Removed auto-close - modal now stays until user clicks Continue Learning
    // setTimeout(() => {
    //     if (document.body.contains(modal)) {
    //         closePhaseCompletion();
    //     }
    // }, 10000);
}
{{ ... }}
